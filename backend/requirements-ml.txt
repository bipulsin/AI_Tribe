# Live-inference extras (ML_MODE=live only).
# Install on top of requirements.txt inside a throwaway container or the
# paperclip-vm ML profile — never as the default local-dev install.
#
#   pip install -r backend/requirements.txt -r backend/requirements-ml.txt
#
# Prefer Python 3.11/3.12. Pin torch to a version with wheels on your platform
# (macOS 12 x86_64 currently resolves to torch 2.2.x; ARM/Linux can use newer).

-r requirements.txt

torch>=2.2.2,<2.6
torchvision>=0.17.2,<0.21
transformers==4.47.1
timm==1.0.12
huggingface-hub==0.27.0
